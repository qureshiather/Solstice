export class ImageService {

    constructor() {
      }

    generateArt(seedString: String, artConfig: any): String {
        /* Generates ART and returns filename of the saved art (save it /tmp) */
        return "FileName";
        // generate art
    }

}

function sketch(p5: any) {
    p5.setup = () => {}
    p5.draw = () => {}
}